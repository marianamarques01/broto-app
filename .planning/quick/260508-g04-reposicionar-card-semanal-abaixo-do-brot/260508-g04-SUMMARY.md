# Quick Task 260508-g04: Reposicionar card semanal abaixo do Broto

**Date:** 2026-05-08
**Status:** Completed

## Summary

- Added `HomeAreaPerformanceCard` with isolated local mock data for area performance bars.
- Moved area performance indicators into the dashboard hero beside `HomePetBanner`.
- Moved `HomeWeeklyProgressCard` below the Broto row and removed its area-performance section.
- Adjusted CSS so the side area card remains readable in desktop layouts and the weekly card spans below the hero.

## Verification

- `npm run typecheck --workspace=@broto/web` passed.
- `npm run build --workspace=@broto/web` passed.
- Cursor lints only reported existing CSS `line-clamp` compatibility warnings outside the changed card styles.

## Review Follow-Up

- Code review flagged possible compression of the area indicators in desktop side layouts.
- Fixed by keeping `.broto-area-performance-card__grid` as a single column when rendered inside `.broto-dashboard-hero__aside`.
