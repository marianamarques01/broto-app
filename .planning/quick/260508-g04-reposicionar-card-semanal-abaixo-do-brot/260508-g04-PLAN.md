---
mode: quick
plan: 260508-g04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/pages/Home.tsx
  - apps/web/src/components/home/HomeWeeklyProgressCard.tsx
  - apps/web/src/components/home/HomeAreaPerformanceCard.tsx
  - apps/web/src/styles/app.css
autonomous: true
must_haves:
  truths:
    - "No desktop, o card do Broto fica na coluna esquerda e os indicadores de desempenho por área ficam ao lado."
    - "O card semanal existente aparece abaixo da linha Broto + desempenho por área."
    - "No mobile, a ordem visual é Broto, desempenho por área, card semanal e depois o fluxo de missões/sidebar."
    - "Dados mockados continuam locais ao componente que os renderiza e fáceis de trocar por dados reais."
  artifacts:
    - path: "apps/web/src/components/home/HomeAreaPerformanceCard.tsx"
      provides: "Indicadores isolados de desempenho por área com mock local"
    - path: "apps/web/src/components/home/HomeWeeklyProgressCard.tsx"
      provides: "Card semanal existente sem a seção de áreas"
    - path: "apps/web/src/pages/Home.tsx"
      provides: "Ordem do layout Home"
    - path: "apps/web/src/styles/app.css"
      provides: "Layout desktop/mobile e estilos dos indicadores"
  key_links:
    - from: "apps/web/src/pages/Home.tsx"
      to: "apps/web/src/components/home/HomeAreaPerformanceCard.tsx"
      via: "import + render dentro de .broto-dashboard-hero__aside"
      pattern: "HomeAreaPerformanceCard"
    - from: "apps/web/src/pages/Home.tsx"
      to: "apps/web/src/components/home/HomeWeeklyProgressCard.tsx"
      via: "render depois de .broto-dashboard-hero"
      pattern: "HomeWeeklyProgressCard"
---

<objective>
Reposicionar o dashboard da Home Web para que o card semanal fique abaixo do Broto e o espaço ao lado do Broto mostre somente os indicadores de desempenho por área.

Purpose: alinhar a composição visual ao layout desejado sem alterar dados reais, bibliotecas ou áreas não relacionadas.
Output: um componente local para desempenho por área, o card semanal simplificado e CSS ajustado para desktop/mobile.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/web/src/pages/Home.tsx
@apps/web/src/components/home/HomeWeeklyProgressCard.tsx
@apps/web/src/styles/app.css

<interfaces>
Home atual:
```typescript
export function Home() {
  // ...
  return (
    <div className="broto-home-dashboard">
      <HomeDashboardTopBar />
      <div className="broto-home-dashboard__body">
        <div className="broto-home-dashboard__main">
          <div className="broto-main-inner broto-main-inner--dashboard">
            <div className="broto-dashboard-grid broto-dashboard-grid--pet-banner broto-fade-in">
              <div className="broto-dashboard-hero">
                <HomePetBanner />
                <div className="broto-dashboard-hero__aside">
                  <HomeWeeklyProgressCard />
                </div>
              </div>
            </div>
          </div>
        </div>
        <HomeRightSidebar />
      </div>
    </div>
  )
}
```

Card semanal atual contém dois blocos de dados:
```typescript
const WEEKLY_PROGRESS_MOCK = {
  metrics: [/* métricas semanais */],
  areas: [/* desempenho por área */],
}
```

Estilos relevantes:
```css
.broto-dashboard-hero {
  display: flex;
  flex-direction: column;
  gap: 22px;
}

@media (min-width: 720px) {
  .broto-dashboard-hero {
    display: grid;
    grid-template-columns: minmax(292px, 336px) minmax(0, 1fr);
  }
}

.broto-weekly-progress-card {
  height: 100%;
  min-height: 360px;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extrair desempenho por área para componente próprio</name>
  <files>apps/web/src/components/home/HomeWeeklyProgressCard.tsx, apps/web/src/components/home/HomeAreaPerformanceCard.tsx</files>
  <behavior>
    - O novo `HomeAreaPerformanceCard` renderiza apenas título "Desempenho por área" e barras de progresso por área.
    - O mock de áreas fica local no novo arquivo, com nome explícito como `HOME_AREA_PERFORMANCE_MOCK`, sem misturar com métricas semanais.
    - `HomeWeeklyProgressCard` preserva o card semanal existente, mas passa a renderizar somente cabeçalho, botão de insights e métricas semanais.
  </behavior>
  <action>
    Crie `HomeAreaPerformanceCard.tsx` usando função exportada nomeada (`export function HomeAreaPerformanceCard()`), mantendo os tipos `WeeklyAreaPerformance` ou equivalentes locais no próprio arquivo. Mova para esse novo componente apenas a seção atualmente em `.broto-weekly-progress-card__areas`, incluindo `CSSProperties` para `--weekly-area-color` e `--weekly-area-percent`.

    Em `HomeWeeklyProgressCard.tsx`, remova `CSSProperties`, o tipo de área, a propriedade `areas` de `WEEKLY_PROGRESS_MOCK` e o JSX de `.broto-weekly-progress-card__areas`. Não altere os textos e visuais das métricas semanais. Não adicione biblioteca nova.
  </action>
  <verify>
    <automated>npx eslint apps/web/src/components/home/HomeWeeklyProgressCard.tsx apps/web/src/components/home/HomeAreaPerformanceCard.tsx</automated>
  </verify>
  <done>O desempenho por área está isolado em componente próprio com mock local, e o card semanal não contém mais a seção de áreas.</done>
</task>

<task type="auto">
  <name>Task 2: Reorganizar a ordem do layout da Home</name>
  <files>apps/web/src/pages/Home.tsx</files>
  <action>
    Importe `HomeAreaPerformanceCard` em `Home.tsx`. Dentro de `.broto-dashboard-hero`, mantenha `HomePetBanner` primeiro e renderize `HomeAreaPerformanceCard` dentro de `.broto-dashboard-hero__aside`, para ocupar somente o espaço ao lado do Broto.

    Mova `HomeWeeklyProgressCard` para depois do fechamento de `.broto-dashboard-hero`, ainda dentro de `.broto-dashboard-grid--pet-banner`. Isso garante a ordem mobile: Broto → desempenho por área → card semanal → `HomeRightSidebar`/missões. Preserve o `PetCard` legado oculto e não toque em componentes de topbar/sidebar.
  </action>
  <verify>
    <automated>npx eslint apps/web/src/pages/Home.tsx apps/web/src/components/home/HomeAreaPerformanceCard.tsx</automated>
  </verify>
  <done>`Home.tsx` renderiza o Broto e áreas lado a lado em telas largas, e o card semanal aparece abaixo no DOM.</done>
</task>

<task type="auto">
  <name>Task 3: Ajustar estilos do layout e dos cards</name>
  <files>apps/web/src/styles/app.css</files>
  <action>
    Atualize apenas os seletores relacionados ao dashboard Home e ao card semanal/áreas.

    Renomeie ou duplique os estilos da seção de áreas para uma classe própria do novo componente, por exemplo `.broto-area-performance-card`, `.broto-area-performance-card__title`, `.broto-area-performance-card__grid`, `.broto-area-performance-card__area`, `.broto-area-performance-card__track` e `.broto-area-performance-card__fill`. Reaproveite a aparência atual das barras, incluindo suporte a tema claro.

    Ajuste `.broto-weekly-progress-card` para funcionar abaixo do hero: remova a dependência visual de `height: 100%`/altura lateral se ela deixar o card alto demais, mantendo o estilo do card. Em desktop, garanta que o card semanal ocupe a largura completa abaixo do hero (`grid-column: 1 / -1` se necessário). Em mobile, não use `order` que contradiga o DOM; a ordem deve vir naturalmente do JSX.

    Preserve os breakpoints existentes (`720px`, `940px`, `1040px`, `1500px`, `560px`) quando possível. Não mexa em estilos de rotas não relacionadas.
  </action>
  <verify>
    <automated>npx eslint apps/web/src/styles/app.css apps/web/src/pages/Home.tsx apps/web/src/components/home/HomeWeeklyProgressCard.tsx apps/web/src/components/home/HomeAreaPerformanceCard.tsx</automated>
    <automated>npm run typecheck --workspace @broto/web</automated>
  </verify>
  <done>Desktop mostra Broto + desempenho por área na primeira linha e card semanal abaixo; mobile segue a ordem Broto, áreas, semanal, missões/sidebar sem overflow horizontal.</done>
</task>

</tasks>

<verification>
Executar:
- `npx eslint apps/web/src/pages/Home.tsx apps/web/src/components/home/HomeWeeklyProgressCard.tsx apps/web/src/components/home/HomeAreaPerformanceCard.tsx`
- `npm run typecheck --workspace @broto/web`

Opcional se o executor abrir a UI localmente: validar visualmente em largura desktop e mobile que a ordem é Broto → desempenho por área → card semanal → missões/sidebar.
</verification>

<success_criteria>
- O card semanal existente está abaixo do card do Broto, não ao lado.
- O espaço ao lado do Broto contém somente indicadores de desempenho por área.
- Mock data de áreas está isolado em `HomeAreaPerformanceCard.tsx` e não acoplado ao mock semanal.
- Nenhuma biblioteca nova foi adicionada.
- Nenhum arquivo sujo não relacionado foi alterado.
</success_criteria>

<output>
After completion, create `.planning/quick/260508-g04-reposicionar-card-semanal-abaixo-do-brot/260508-g04-SUMMARY.md`
</output>
