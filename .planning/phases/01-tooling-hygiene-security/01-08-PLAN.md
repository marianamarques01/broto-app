---
phase: 01-tooling-hygiene-security
plan: 08
type: execute
wave: 5
depends_on: [01-07]
files_modified:
  - apps/mobile/hooks/useAuth.ts
  - apps/mobile/hooks/usePet.ts
  - apps/mobile/hooks/useProgress.ts
  - apps/mobile/hooks/useUser.ts
  - apps/mobile/hooks/useClass.ts
  - apps/mobile/hooks/useQuestionsFilters.ts
  - apps/mobile/hooks/createCachedHook.ts
  - "apps/mobile/**/*.{ts,tsx} (import updates only)"
autonomous: true
requirements: [TOOL-07]

must_haves:
  truths:
    - "All target mobile hook files use camelCase filenames"
    - "No imports reference kebab-case hook paths"
    - "Mobile typecheck resolves renamed hook modules"
  artifacts:
    - path: "apps/mobile/hooks/useAuth.ts"
      provides: "Renamed hook from use-auth.ts"
    - path: "apps/mobile/hooks/createCachedHook.ts"
      provides: "Renamed hook from create-cached-hook.ts"
  key_links:
    - from: "apps/mobile/**/*.{ts,tsx}"
      to: "apps/mobile/hooks/*"
      via: "updated import specifiers"
      pattern: "from '@/hooks/useAuth' (and peers)"
---

<objective>
Rename mobile hooks from kebab-case to camelCase and update all imports.

Purpose: Per D-09, naming must align with web/admin conventions; this plan is isolated from formatting and blame-ignore metadata to keep diffs focused.
Output: One commit with file renames + import rewrites only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renomear hooks e corrigir todos os imports (D-09)</name>
  <files>
    apps/mobile/hooks/useAuth.ts
    apps/mobile/hooks/usePet.ts
    apps/mobile/hooks/useProgress.ts
    apps/mobile/hooks/useUser.ts
    apps/mobile/hooks/useClass.ts
    apps/mobile/hooks/useQuestionsFilters.ts
    apps/mobile/hooks/createCachedHook.ts
    apps/mobile/**/*.{ts,tsx}
  </files>
  <action>
    1) Renomear arquivos com `git mv`:
    - `use-auth.ts` -> `useAuth.ts`
    - `use-pet.ts` -> `usePet.ts`
    - `use-progress.ts` -> `useProgress.ts`
    - `use-user.ts` -> `useUser.ts`
    - `use-class.ts` -> `useClass.ts`
    - `use-questions-filters.ts` -> `useQuestionsFilters.ts`
    - `create-cached-hook.ts` -> `createCachedHook.ts`

    2) Atualizar todos os imports em `apps/mobile/**/*.{ts,tsx}` para os novos paths.

    3) Garantir ausência de paths antigos:
    ```
    rg "hooks/(use-auth|use-pet|use-progress|use-user|use-class|use-questions-filters|create-cached-hook)" apps/mobile
    ```
    Deve retornar vazio.

    4) Validar resolução TS:
    ```
    npx tsc --noEmit --project apps/mobile/tsconfig.json
    ```

    5) Commit isolado:
    ```
    git add -A
    git commit -m "chore(tooling): rename mobile hooks to camelCase (TOOL-07)"
    ```
  </action>
  <verify>
    <automated>rg "hooks/(use-auth|use-pet|use-progress|use-user|use-class|use-questions-filters|create-cached-hook)" apps/mobile | wc -l</automated>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>Os 7 hooks foram renomeados para camelCase e todos os consumidores do mobile apontam para os novos caminhos.</done>
</task>

</tasks>

<verification>
1. `ls apps/mobile/hooks` mostra apenas os nomes camelCase esperados.
2. `rg "hooks/(use-auth|use-pet|use-progress|use-user|use-class|use-questions-filters|create-cached-hook)" apps/mobile` retorna vazio.
3. `npx tsc --noEmit --project apps/mobile/tsconfig.json` sai com código 0.
</verification>

<success_criteria>
- Nenhum import restante usa nome kebab-case.
- Todos os hooks alvo existem com nome camelCase.
- Typecheck do mobile permanece funcional após renames.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tooling-hygiene-security/01-08-SUMMARY.md` with:
- lista old -> new dos 7 hooks
- arquivos consumidores atualizados
- hash do commit
</output>
