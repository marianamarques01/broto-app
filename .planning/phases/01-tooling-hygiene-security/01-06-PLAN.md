---
phase: 01-tooling-hygiene-security
plan: 06
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - apps/mobile/assets/new-logo-icon.svg
  - apps/web/public/new-logo-icon.svg
  - apps/web/public/new-logo.svg
autonomous: true
gap_closure: true
requirements: [HYGN-02]

must_haves:
  truths:
    - "Os 3 SVGs de aplicativo existem somente em diretórios de app e têm redução de tamanho real em relação ao baseline"
    - "Cada SVG alvo fica dentro do orçamento de tamanho definido para fechamento do gap HYGN-02"
    - "Os SVGs continuam válidos (com tag <svg) e sem fallback para cópias na raiz"
  artifacts:
    - path: "apps/mobile/assets/new-logo-icon.svg"
      provides: "Ícone otimizado usado pelo app mobile"
      max_bytes: 102400
    - path: "apps/web/public/new-logo-icon.svg"
      provides: "Ícone otimizado usado pelo app web"
      max_bytes: 102400
    - path: "apps/web/public/new-logo.svg"
      provides: "Logo otimizada usada pelo app web"
      max_bytes: 153600
  key_links:
    - from: "01-VERIFICATION.md gap(HYGN-02)"
      to: "apps/mobile/assets/new-logo-icon.svg"
      via: "otimização SVGO + validação por tamanho em bytes"
      pattern: "<= 102400 bytes"
    - from: "01-VERIFICATION.md gap(HYGN-02)"
      to: "apps/web/public/new-logo.svg"
      via: "otimização SVGO + validação por tamanho em bytes"
      pattern: "<= 153600 bytes"
---

<objective>
Fechar o gap de verificação HYGN-02 reduzindo de forma mensurável o tamanho dos SVGs finais dos apps, sem reintroduzir duplicatas na raiz.

Purpose: A Fase 01 ficou em 5/6 truths porque os SVGs permaneceram em 723KB/1.1MB; este plano fecha exclusivamente essa lacuna com orçamento objetivo por arquivo e comandos repetíveis.
Output: Três SVGs otimizados (`apps/mobile/assets/new-logo-icon.svg`, `apps/web/public/new-logo-icon.svg`, `apps/web/public/new-logo.svg`) com validação automatizada de tamanho e integridade.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tooling-hygiene-security/1-CONTEXT.md
@.planning/phases/01-tooling-hygiene-security/01-RESEARCH.md
@.planning/phases/01-tooling-hygiene-security/01-VERIFICATION.md
@.planning/phases/01-tooling-hygiene-security/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-otimizar os SVGs com configuração agressiva e baseline explícito (D-02)</name>
  <files>
    apps/mobile/assets/new-logo-icon.svg
    apps/web/public/new-logo-icon.svg
    apps/web/public/new-logo.svg
  </files>
  <read_first>
    - apps/mobile/assets/new-logo-icon.svg
    - apps/web/public/new-logo-icon.svg
    - apps/web/public/new-logo.svg
    - .planning/phases/01-tooling-hygiene-security/01-VERIFICATION.md (baseline atual: 723KB, 723KB, 1.1MB)
    - .planning/phases/01-tooling-hygiene-security/1-CONTEXT.md (D-02: usar `npx svgo`, remover duplicatas da raiz)
  </read_first>
  <action>
    Executar uma nova rodada de otimização com baseline salvo e configuração SVGO explícita.

    1) Registrar baseline em bytes (antes de editar):
    ```bash
    stat -f "%N %z" apps/mobile/assets/new-logo-icon.svg apps/web/public/new-logo-icon.svg apps/web/public/new-logo.svg
    ```

    2) Rodar SVGO multipass com preset-default + plugins explícitos:
    ```bash
    npx svgo --multipass \
      --config='{"plugins":[{"name":"preset-default"},{"name":"removeDimensions"},{"name":"removeXMLNS"},{"name":"sortAttrs"},{"name":"cleanupIds"}]}' \
      apps/mobile/assets/new-logo-icon.svg apps/web/public/new-logo-icon.svg apps/web/public/new-logo.svg
    ```

    3) Se algum arquivo continuar acima do budget, rodar segunda passada com plugin de minificação de dados de path:
    ```bash
    npx svgo --multipass \
      --config='{"plugins":[{"name":"preset-default"},{"name":"convertPathData","params":{"floatPrecision":2}},{"name":"mergePaths"},{"name":"sortAttrs"}]}' \
      apps/mobile/assets/new-logo-icon.svg apps/web/public/new-logo-icon.svg apps/web/public/new-logo.svg
    ```

    4) Verificar orçamento-alvo por arquivo (fechamento do gap):
    - `apps/mobile/assets/new-logo-icon.svg` <= 102400 bytes
    - `apps/web/public/new-logo-icon.svg` <= 102400 bytes
    - `apps/web/public/new-logo.svg` <= 153600 bytes

    5) Confirmar que os 3 arquivos continuam SVG válidos (contêm `<svg` na primeira janela de leitura textual):
    ```bash
    rg "<svg" apps/mobile/assets/new-logo-icon.svg apps/web/public/new-logo-icon.svg apps/web/public/new-logo.svg
    ```
  </action>
  <verify>
    <automated>stat -f "%z" apps/mobile/assets/new-logo-icon.svg</automated>
    <automated>stat -f "%z" apps/web/public/new-logo-icon.svg</automated>
    <automated>stat -f "%z" apps/web/public/new-logo.svg</automated>
    <automated>rg "&lt;svg" apps/mobile/assets/new-logo-icon.svg apps/web/public/new-logo-icon.svg apps/web/public/new-logo.svg</automated>
  </verify>
  <acceptance_criteria>
    - `stat -f "%z" apps/mobile/assets/new-logo-icon.svg` retorna valor <= `102400`
    - `stat -f "%z" apps/web/public/new-logo-icon.svg` retorna valor <= `102400`
    - `stat -f "%z" apps/web/public/new-logo.svg` retorna valor <= `153600`
    - `rg "<svg" apps/mobile/assets/new-logo-icon.svg` retorna pelo menos 1 match
    - `rg "<svg" apps/web/public/new-logo-icon.svg` retorna pelo menos 1 match
    - `rg "<svg" apps/web/public/new-logo.svg` retorna pelo menos 1 match
  </acceptance_criteria>
  <done>Os três SVGs foram otimizados com redução mensurável e atendem o orçamento de bytes definido para fechamento de HYGN-02.</done>
</task>

<task type="auto">
  <name>Task 2: Validar ausência de duplicatas na raiz e atualizar evidência do gap fechado</name>
  <files>
    apps/mobile/assets/new-logo-icon.svg
    apps/web/public/new-logo-icon.svg
    apps/web/public/new-logo.svg
  </files>
  <read_first>
    - .planning/phases/01-tooling-hygiene-security/01-VERIFICATION.md (gap aberto de HYGN-02)
    - apps/mobile/assets/new-logo-icon.svg
    - apps/web/public/new-logo-icon.svg
    - apps/web/public/new-logo.svg
  </read_first>
  <action>
    Executar validação final com checks determinísticos para garantir que o fechamento do gap não reintroduziu anti-patterns.

    1) Confirmar que não existem cópias na raiz:
    ```bash
    test ! -f 2.svg && test ! -f new_logo.svg && test ! -f new_logo_icon.svg
    ```

    2) Confirmar que os três arquivos alvo ainda existem nos apps:
    ```bash
    test -f apps/mobile/assets/new-logo-icon.svg
    test -f apps/web/public/new-logo-icon.svg
    test -f apps/web/public/new-logo.svg
    ```

    3) Revalidar tamanhos finais em bytes:
    ```bash
    stat -f "%N %z" apps/mobile/assets/new-logo-icon.svg apps/web/public/new-logo-icon.svg apps/web/public/new-logo.svg
    ```

    4) Rodar check rápido de formatação nesses SVGs para evitar ruído de diff inválido:
    ```bash
    npx prettier --check apps/mobile/assets/new-logo-icon.svg apps/web/public/new-logo-icon.svg apps/web/public/new-logo.svg
    ```
  </action>
  <verify>
    <automated>test ! -f 2.svg &amp;&amp; test ! -f new_logo.svg &amp;&amp; test ! -f new_logo_icon.svg &amp;&amp; echo "root-clean"</automated>
    <automated>test -f apps/mobile/assets/new-logo-icon.svg &amp;&amp; test -f apps/web/public/new-logo-icon.svg &amp;&amp; test -f apps/web/public/new-logo.svg &amp;&amp; echo "app-files-present"</automated>
    <automated>npx prettier --check apps/mobile/assets/new-logo-icon.svg apps/web/public/new-logo-icon.svg apps/web/public/new-logo.svg</automated>
  </verify>
  <acceptance_criteria>
    - comando `test ! -f 2.svg && test ! -f new_logo.svg && test ! -f new_logo_icon.svg` sai com código `0`
    - comando `test -f apps/mobile/assets/new-logo-icon.svg && test -f apps/web/public/new-logo-icon.svg && test -f apps/web/public/new-logo.svg` sai com código `0`
    - `npx prettier --check apps/mobile/assets/new-logo-icon.svg apps/web/public/new-logo-icon.svg apps/web/public/new-logo.svg` sai com código `0`
    - saída de `stat -f "%N %z" ...` confirma os limites: `102400`, `102400`, `153600` bytes
  </acceptance_criteria>
  <done>O gap HYGN-02 está fechado com evidência verificável: arquivos otimizados, sem duplicatas na raiz e com validação automatizada repetível.</done>
</task>

</tasks>

<verification>
1. `stat -f "%N %z" apps/mobile/assets/new-logo-icon.svg apps/web/public/new-logo-icon.svg apps/web/public/new-logo.svg` deve mostrar tamanhos dentro dos budgets.
2. `test ! -f 2.svg && test ! -f new_logo.svg && test ! -f new_logo_icon.svg` deve sair 0.
3. `rg "<svg" apps/mobile/assets/new-logo-icon.svg apps/web/public/new-logo-icon.svg apps/web/public/new-logo.svg` deve retornar match em todos.
4. `npx prettier --check apps/mobile/assets/new-logo-icon.svg apps/web/public/new-logo-icon.svg apps/web/public/new-logo.svg` deve sair 0.
</verification>

<success_criteria>
- O requisito `HYGN-02` passa de `partial` para `satisfied` no próximo ciclo de verificação.
- Nenhum dos 3 SVGs de app ultrapassa o limite de bytes definido neste plano.
- Não existem SVGs duplicados na raiz (`2.svg`, `new_logo.svg`, `new_logo_icon.svg` permanecem ausentes).
- O plano fecha o único gap remanescente da Fase 01 sem alterar regras de tooling ou lógica de negócio.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tooling-hygiene-security/01-06-SUMMARY.md` contendo:
- baseline em bytes (antes) e tamanhos finais (depois) para os 3 SVGs;
- comandos SVGO executados (incluindo a segunda passada, se usada);
- evidências de checks (`stat`, `rg "<svg"`, `prettier --check`, ausência de SVGs na raiz);
- hash do commit de fechamento de `HYGN-02`.
</output>
