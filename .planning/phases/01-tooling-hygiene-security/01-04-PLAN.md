---
phase: 01-tooling-hygiene-security
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - "Arquivos tocados por `npm run format` (capturar lista real via `git diff --name-only` antes do commit de style)"
autonomous: true
requirements: [TOOL-04]

must_haves:
  truths:
    - "Running npm run format:check exits 0 — all source files conform to Prettier config"
    - "The formatting commit is isolated (style-only changes, no renames or logic edits)"
  artifacts:
    - path: "Arquivos modificados por npm run format"
      provides: "Estilo padronizado no monorepo"
      contains: "mudanças de formatação apenas"
  key_links:
    - from: "package.json script format"
      to: "arquivos apps/mobile + apps/web + apps/admin + packages + supabase/functions"
      via: "npm run format"
      pattern: "prettier --write"
---

<objective>
Run only the atomic format commit across all source files.

Purpose: Per D-08, the formatting commit must stay atomic and isolated, without mixing blame-ignore metadata or filename renames. This keeps a clean, reviewable style-only diff.
Output: One style-only commit covering repository formatting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run atomic format commit (style-only) across monorepo</name>
  <files>
    "Arquivos retornados por `git diff --name-only` após `npm run format` (capturados no commit de style, sem renames)"
  </files>
  <read_first>
    - Run: npm run format:check 2>&1 | head -20 (see which files are flagged — must include mobile files)
    - Confirm Plan 03 is complete: grep "apps/mobile/{app,hooks" package.json must return a match
  </read_first>
  <action>
    PREREQUISITE CHECK: Confirm Plan 03 is done before starting.
    ```
    grep "apps/mobile/{app,hooks" package.json
    ```
    If this returns nothing, Plan 03 has not been executed. Stop and run Plan 03 first.

    Step 1 — Run format:check to confirm violations exist (expected before formatting):
    ```
    npm run format:check 2>&1 | grep "apps/mobile" | wc -l
    ```
    Should return > 0. If 0, the glob is not working — stop and debug.

    Step 2 — Run the format write command (this touches ALL source files in the glob):
    ```
    npm run format
    ```

    Step 3 — Verify format:check now passes:
    ```
    npm run format:check
    ```
    Must exit 0. If not, some files are not covered — check the error output and re-run format.

    Step 4 — This commit must contain ONLY formatting changes. Verify no logic was changed:
    ```
    git diff --name-only | head -20
    ```
    All listed files should be source files with only quote/semicolon/indent changes. No new files, no deleted files.

    Step 5 — Create the atomic format commit (per D-08):
    ```
    git add -A
    git commit -m "style: apply Prettier formatting across all apps (TOOL-02, TOOL-04)"
    ```
  </action>
  <verify>
    <automated>npm run format:check</automated>
    Expected: exit 0 (all files conform)
    <automated>git show --name-status --pretty="" HEAD | rg -v "^[AM]\s" | wc -l</automated>
    Expected: 0 (no unexpected status lines; only modified/added style files)
  </verify>
  <acceptance_criteria>
    - `npm run format:check` exits 0 with no output
    - `git show --name-status --pretty="" HEAD | rg "^R" | wc -l` retorna `0` (sem renames neste plano)
    - `git show --name-status --pretty="" HEAD | rg "^D" | wc -l` retorna `0` (sem deleções neste plano)
  </acceptance_criteria>
  <done>All source files are formatted. The commit is isolated and contém apenas mudanças de estilo (D-08).</done>
</task>

</tasks>

<verification>
After Task 1:
1. `npm run format:check` — exits 0
2. `git show --name-status --pretty="" HEAD | rg "^R"` — returns empty
3. `git show --name-status --pretty="" HEAD | rg "^D"` — returns empty
</verification>

<success_criteria>
- npm run format:check exits 0 across all apps including mobile
- Format commit is isolated (pure style changes only, per D-08)
- No blame-ignore metadata or hook renames are mixed into this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-tooling-hygiene-security/01-04-SUMMARY.md` with:
- Format commit hash (the exact 40-char SHA)
- Number of mobile files reformatted
- Diff sanity evidence proving this commit is style-only
</output>
