---
phase: 01-tooling-hygiene-security
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - ".git-blame-ignore-revs"
  - "apps/mobile/**/*.{ts,tsx} (formatting — all mobile source files)"
  - "apps/mobile/hooks/useAuth.ts (renamed from use-auth.ts)"
  - "apps/mobile/hooks/usePet.ts (renamed from use-pet.ts)"
  - "apps/mobile/hooks/useProgress.ts (renamed from use-progress.ts)"
  - "apps/mobile/hooks/useUser.ts (renamed from use-user.ts)"
  - "apps/mobile/hooks/useClass.ts (renamed from use-class.ts)"
  - "apps/mobile/hooks/useQuestionsFilters.ts (renamed from use-questions-filters.ts)"
  - "apps/mobile/hooks/createCachedHook.ts (renamed from create-cached-hook.ts)"
  - "apps/mobile/**/*.{ts,tsx} (import updates for renamed hooks)"
autonomous: true
requirements: [TOOL-04, TOOL-07]

must_haves:
  truths:
    - "Running npm run format:check exits 0 — all source files conform to Prettier config"
    - "The format commit hash is recorded in .git-blame-ignore-revs"
    - "All 7 mobile hooks are camelCase named files"
    - "No remaining imports reference the old kebab-case hook filenames"
  artifacts:
    - path: ".git-blame-ignore-revs"
      provides: "Format commit hash registration"
      contains: "Formatting-only commits"
    - path: "apps/mobile/hooks/useAuth.ts"
      provides: "Renamed hook (was use-auth.ts)"
    - path: "apps/mobile/hooks/usePet.ts"
      provides: "Renamed hook (was use-pet.ts)"
  key_links:
    - from: ".git-blame-ignore-revs"
      to: "format commit hash"
      via: "git blame --ignore-revs-file"
      pattern: "[0-9a-f]{40}"
---

<objective>
Run the atomic format commit across all source files, register it in .git-blame-ignore-revs, then rename mobile hooks from kebab-case to camelCase and update all callers.

Purpose: The format commit (TOOL-04) must be isolated — zero logic changes — so git blame remains useful. Hook renaming (TOOL-07) comes AFTER to avoid mixing rename history with style changes (per D-08, D-09 and Anti-Pattern #2 in RESEARCH.md). After this plan, the codebase is consistently formatted and hook naming matches web conventions.
Output: Two distinct commits — one pure format, one pure rename — plus .git-blame-ignore-revs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run atomic format commit and register hash in .git-blame-ignore-revs</name>
  <files>
    .git-blame-ignore-revs
    "apps/mobile/**/*.{ts,tsx} (all mobile source files — content only, no renames)"
  </files>
  <read_first>
    - Run: npm run format:check 2>&1 | head -20 (see which files are flagged — must include mobile files)
    - Confirm Plan 03 is complete: grep "apps/mobile/{app,hooks" package.json must return a match
  </read_first>
  <action>
    PREREQUISITE CHECK: Confirm Plan 03 is done before starting.
    ```
    grep "apps/mobile/{app,hooks" package.json
    ```
    If this returns nothing, Plan 03 has not been executed. Stop and run Plan 03 first.

    Step 1 — Run format:check to confirm violations exist (expected before formatting):
    ```
    npm run format:check 2>&1 | grep "apps/mobile" | wc -l
    ```
    Should return > 0. If 0, the glob is not working — stop and debug.

    Step 2 — Run the format write command (this touches ALL source files in the glob):
    ```
    npm run format
    ```

    Step 3 — Verify format:check now passes:
    ```
    npm run format:check
    ```
    Must exit 0. If not, some files are not covered — check the error output and re-run format.

    Step 4 — This commit must contain ONLY formatting changes. Verify no logic was changed:
    ```
    git diff --name-only | head -20
    ```
    All listed files should be source files with only quote/semicolon/indent changes. No new files, no deleted files.

    Step 5 — Create the atomic format commit:
    ```
    git add -A
    git commit -m "style: apply Prettier formatting across all apps (TOOL-02, TOOL-04)"
    ```
    Record the commit hash:
    ```
    FORMAT_HASH=$(git rev-parse HEAD)
    echo "Format commit: $FORMAT_HASH"
    ```

    Step 6 — Create `.git-blame-ignore-revs` at repo root (per Pattern 5 in RESEARCH.md):
    ```
    # .git-blame-ignore-revs
    # Formatting-only commits — skip these when running git blame
    # See: https://git-scm.com/docs/git-blame#Documentation/git-blame.txt---ignore-revs-fileltfilegt
    <FORMAT_HASH>
    ```
    Replace `<FORMAT_HASH>` with the actual hash from step 5.

    Step 7 — Configure git to use this file automatically:
    ```
    git config blame.ignoreRevsFile .git-blame-ignore-revs
    ```

    Step 8 — Commit the ignore-revs file:
    ```
    git add .git-blame-ignore-revs
    git commit -m "chore(tooling): register format commit in .git-blame-ignore-revs (TOOL-04)"
    ```
  </action>
  <verify>
    <automated>npm run format:check</automated>
    Expected: exit 0 (all files conform)
    <automated>cat .git-blame-ignore-revs</automated>
    Expected: file exists and contains a 40-character hex commit hash
  </verify>
  <acceptance_criteria>
    - `npm run format:check` exits 0 with no output
    - `ls .git-blame-ignore-revs` succeeds (file exists)
    - `cat .git-blame-ignore-revs | grep -E "[0-9a-f]{40}"` returns exactly one hash line
    - The hash in `.git-blame-ignore-revs` matches the format commit: `git log --oneline | grep "apply Prettier"` shows the same short hash
    - `grep "Formatting-only commits" .git-blame-ignore-revs` returns the comment line
  </acceptance_criteria>
  <done>All source files are formatted. The format commit is isolated (no logic changes). The commit hash is registered in .git-blame-ignore-revs. Git blame will skip this commit.</done>
</task>

<task type="auto">
  <name>Task 2: Rename mobile hooks from kebab-case to camelCase and update all callers</name>
  <files>
    apps/mobile/hooks/useAuth.ts
    apps/mobile/hooks/usePet.ts
    apps/mobile/hooks/useProgress.ts
    apps/mobile/hooks/useUser.ts
    apps/mobile/hooks/useClass.ts
    apps/mobile/hooks/useQuestionsFilters.ts
    apps/mobile/hooks/createCachedHook.ts
    "apps/mobile/**/*.{ts,tsx} (import updates)"
  </files>
  <read_first>
    - Run: ls apps/mobile/hooks/ (see current filenames — 7 kebab-case files)
    - Run: grep -rn "use-auth\|use-pet\|use-progress\|use-user\|use-class\|use-questions-filters\|create-cached-hook" apps/mobile/ (find all callers)
  </read_first>
  <action>
    PREREQUISITE: Task 1 must be complete (format commit done). Renames happen AFTER formatting.

    Step 1 — Identify all callers. Run:
    ```
    grep -rn "from.*hooks/use-" apps/mobile/
    grep -rn "from.*hooks/create-cached-hook" apps/mobile/
    ```
    Note every file and the exact import path used.

    Step 2 — Rename using git mv to preserve history (per D-09):
    ```
    git mv apps/mobile/hooks/use-auth.ts apps/mobile/hooks/useAuth.ts
    git mv apps/mobile/hooks/use-pet.ts apps/mobile/hooks/usePet.ts
    git mv apps/mobile/hooks/use-progress.ts apps/mobile/hooks/useProgress.ts
    git mv apps/mobile/hooks/use-user.ts apps/mobile/hooks/useUser.ts
    git mv apps/mobile/hooks/use-class.ts apps/mobile/hooks/useClass.ts
    git mv apps/mobile/hooks/use-questions-filters.ts apps/mobile/hooks/useQuestionsFilters.ts
    git mv apps/mobile/hooks/create-cached-hook.ts apps/mobile/hooks/createCachedHook.ts
    ```

    Step 3 — Update every caller found in Step 1. Replace old paths with new paths:
    - `from '@/hooks/use-auth'` → `from '@/hooks/useAuth'`
    - `from '@/hooks/use-pet'` → `from '@/hooks/usePet'`
    - `from '@/hooks/use-progress'` → `from '@/hooks/useProgress'`
    - `from '@/hooks/use-user'` → `from '@/hooks/useUser'`
    - `from '@/hooks/use-class'` → `from '@/hooks/useClass'`
    - `from '@/hooks/use-questions-filters'` → `from '@/hooks/useQuestionsFilters'`
    - `from '@/hooks/create-cached-hook'` → `from '@/hooks/createCachedHook'`
    Also handle relative imports (`from '../hooks/use-auth'` → `from '../hooks/useAuth'`, etc.)

    Step 4 — Verify no old references remain:
    ```
    grep -r "hooks/use-auth\|hooks/use-pet\|hooks/use-progress\|hooks/use-user\|hooks/use-class\|hooks/use-questions-filters\|hooks/create-cached-hook" apps/mobile/
    ```
    Must return empty.

    Step 5 — Verify TypeScript still resolves the renamed files:
    ```
    npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep "Cannot find module.*hooks" | head -10
    ```
    Must return empty (no missing module errors for hooks).

    Step 6 — Commit:
    ```
    git add -A
    git commit -m "chore(tooling): rename mobile hooks to camelCase (TOOL-07)"
    ```
  </action>
  <verify>
    <automated>ls apps/mobile/hooks/</automated>
    Expected: useAuth.ts, usePet.ts, useProgress.ts, useUser.ts, useClass.ts, useQuestionsFilters.ts, createCachedHook.ts
    <automated>grep -r "hooks/use-auth\|hooks/use-pet\|hooks/use-progress\|hooks/use-user\|hooks/use-class\|hooks/use-questions-filters\|hooks/create-cached-hook" apps/mobile/ | wc -l</automated>
    Expected: 0
  </verify>
  <acceptance_criteria>
    - `ls apps/mobile/hooks/useAuth.ts` succeeds
    - `ls apps/mobile/hooks/usePet.ts` succeeds
    - `ls apps/mobile/hooks/useProgress.ts` succeeds
    - `ls apps/mobile/hooks/useUser.ts` succeeds
    - `ls apps/mobile/hooks/useClass.ts` succeeds
    - `ls apps/mobile/hooks/useQuestionsFilters.ts` succeeds
    - `ls apps/mobile/hooks/createCachedHook.ts` succeeds
    - `ls apps/mobile/hooks/use-auth.ts 2>/dev/null` returns nothing (old names gone)
    - `grep -r "hooks/use-auth" apps/mobile/` returns empty
    - `grep -r "hooks/use-pet" apps/mobile/` returns empty
    - `npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | grep "Cannot find module.*hooks"` returns empty
  </acceptance_criteria>
  <done>All 7 mobile hooks renamed to camelCase. All callers updated. No old kebab-case references remain. TypeScript resolves the renamed files correctly.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run format:check` — exits 0
2. `cat .git-blame-ignore-revs | grep -E "[0-9a-f]{40}"` — returns one hash
3. `ls apps/mobile/hooks/` — shows only camelCase filenames
4. `grep -r "hooks/use-auth\|hooks/use-pet" apps/mobile/` — returns empty
</verification>

<success_criteria>
- npm run format:check exits 0 across all apps including mobile
- Format commit is isolated (pure style changes only)
- .git-blame-ignore-revs exists at repo root with format commit hash
- All 7 mobile hooks have camelCase filenames
- Zero remaining imports reference old kebab-case hook names
</success_criteria>

<output>
After completion, create `.planning/phases/01-tooling-hygiene-security/01-04-SUMMARY.md` with:
- Format commit hash (the exact 40-char SHA)
- Number of mobile files reformatted
- List of all 7 renamed hook files with old → new names
- List of all caller files updated (with file paths)
- Commit hashes for all 3 commits (format, ignore-revs, rename)
</output>
