---
phase: 01-tooling-hygiene-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - apps/web/public/new_logo.svg
  - apps/mobile/assets/new-logo-icon.svg
  - apps/web/package.json
  - packages/ui/ (removal)
  - 2.svg (removal)
  - new_logo.svg (removal)
  - new_logo_icon.svg (removal)
autonomous: true
requirements: [HYGN-01, HYGN-02, HYGN-03]

must_haves:
  truths:
    - "git ls-files | grep .venv returns nothing — Python environment fully untracked"
    - "SVG files in app directories are optimized (under 50KB each)"
    - "Root-level SVG duplicates removed"
    - "packages/ui is either removed or has a description field in its package.json"
    - "apps/web/package.json no longer lists @broto/ui as a dependency"
  artifacts:
    - path: ".gitignore"
      provides: "Excludes .venv/ and __pycache__/"
      contains: ".venv"
    - path: "apps/web/public/new_logo.svg"
      provides: "Optimized web logo"
    - path: "apps/mobile/assets/new-logo-icon.svg"
      provides: "Optimized mobile logo"
  key_links:
    - from: "apps/web/package.json"
      to: "packages/ui"
      via: "@broto/ui workspace dep"
      pattern: "@broto/ui"
---

<objective>
Remove Python environment from git tracking, optimize SVG assets, and audit/remove the dead packages/ui package.

Purpose: These are pure repo hygiene tasks with zero risk of breakage. They reduce repo bloat (3,306 tracked Python files, 1MB+ SVGs) and eliminate dead code before any logic changes occur.
Output: Clean git index, optimized assets, no dead packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Untrack Python .venv and add to .gitignore</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (see current contents before adding entries)
  </read_first>
  <action>
    Step 1 — Remove tracked files (does NOT delete from disk, only removes from git index):
    ```
    git rm -r --cached supabase/services/notebooklm/.venv/ 2>/dev/null || true
    git rm -r --cached .venv/ 2>/dev/null || true
    git rm -r --cached __pycache__/ 2>/dev/null || true
    find . -name "__pycache__" -not -path "./node_modules/*" | xargs git rm -r --cached 2>/dev/null || true
    ```

    Step 2 — Add to .gitignore. Append these lines (per D-01):
    ```
    # Python
    .venv/
    __pycache__/
    *.pyc
    *.pyo
    ```

    Step 3 — Verify:
    ```
    git ls-files | grep -E "\.venv|__pycache__"
    ```
    This must return EMPTY. If anything is still listed, re-run the git rm command for that path.

    Step 4 — Stage and commit:
    ```
    git add .gitignore
    git commit -m "chore(hygiene): untrack Python .venv and __pycache__ (HYGN-01)"
    ```
    Do NOT amend — create a new commit.
  </action>
  <verify>
    <automated>git ls-files | grep -E "\.venv|__pycache__" | wc -l</automated>
    Expected output: 0 (empty — no matches)
  </verify>
  <acceptance_criteria>
    - `git ls-files | grep ".venv"` returns empty (exit code 1 with no output)
    - `git ls-files | grep "__pycache__"` returns empty
    - `.gitignore` contains the line `.venv/`
    - `.gitignore` contains the line `__pycache__/`
    - `grep -c "\.venv" .gitignore` returns >= 1
  </acceptance_criteria>
  <done>Python environment files are fully untracked. The .venv directory still exists on disk but git no longer knows about it.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize SVG assets and remove root duplicates</name>
  <files>
    apps/web/public/new_logo.svg
    apps/mobile/assets/new-logo-icon.svg
    2.svg (root — to delete)
    new_logo.svg (root — to delete)
    new_logo_icon.svg (root — to delete)
  </files>
  <read_first>
    - Run: ls -la *.svg apps/web/public/*.svg apps/mobile/assets/*.svg (see current sizes)
    - Run: diff new_logo.svg apps/web/public/new_logo.svg (check if root and web copy are identical)
  </read_first>
  <action>
    Step 1 — Check current SVG sizes:
    ```
    ls -lh 2.svg new_logo.svg new_logo_icon.svg apps/web/public/new_logo.svg apps/mobile/assets/new-logo-icon.svg 2>/dev/null
    ```

    Step 2 — Optimize app-directory SVGs in place (per D-02, using npx svgo --multipass):
    ```
    npx svgo --multipass apps/web/public/new_logo.svg
    npx svgo --multipass apps/mobile/assets/new-logo-icon.svg
    ```
    If there are additional SVGs in apps/web/public/ or apps/mobile/assets/, optimize them too:
    ```
    find apps/web/public -name "*.svg" -exec npx svgo --multipass {} \;
    find apps/mobile/assets -name "*.svg" -exec npx svgo --multipass {} \;
    ```

    Step 3 — Remove root-level SVG duplicates (per D-02). Before removing each, verify:
    - `diff new_logo.svg apps/web/public/new_logo.svg` — if identical or root is the unoptimized original, safe to remove
    - Root `new_logo_icon.svg` maps to `apps/mobile/assets/new-logo-icon.svg` (note: hyphen vs underscore variant name — these are the same asset)
    - Root `2.svg` is a standalone file — remove it (no app directory counterpart needed)

    Remove root duplicates:
    ```
    git rm 2.svg new_logo.svg new_logo_icon.svg
    ```

    Step 4 — Stage and commit:
    ```
    git add apps/web/public/ apps/mobile/assets/
    git commit -m "chore(hygiene): optimize SVG assets with SVGO, remove root duplicates (HYGN-02)"
    ```
  </action>
  <verify>
    <automated>ls -la apps/web/public/*.svg apps/mobile/assets/*.svg 2>/dev/null</automated>
    Expected: files exist and are significantly smaller than before
    <automated>ls 2.svg new_logo.svg new_logo_icon.svg 2>/dev/null | wc -l</automated>
    Expected: 0 (files removed from repo)
  </verify>
  <acceptance_criteria>
    - `ls 2.svg` returns "No such file or directory" (removed from repo)
    - `ls new_logo.svg` returns "No such file or directory"
    - `ls new_logo_icon.svg` returns "No such file or directory"
    - `ls apps/web/public/new_logo.svg` succeeds (app copy still present)
    - `ls apps/mobile/assets/new-logo-icon.svg` succeeds (app copy still present)
    - Post-optimization SVG files are smaller than pre-optimization sizes (verify with `ls -lh`)
    - `git ls-files | grep "^2\.svg"` returns empty
  </acceptance_criteria>
  <done>SVGs are optimized in app directories. Root-level duplicate SVGs are removed. App functionality unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: Audit and remove packages/ui</name>
  <files>
    apps/web/package.json
    packages/ui/ (directory removal)
  </files>
  <read_first>
    - apps/web/package.json (see current dependencies section)
    - packages/ui/package.json (see current contents before removal)
    - Run: grep -r "@broto/ui" apps/ (confirm no source-file imports — per D-03 and Pitfall #5 in research)
  </read_first>
  <action>
    Step 1 — Confirm no source imports exist (per D-03):
    ```
    grep -r "@broto/ui" apps/web/src/ apps/mobile/ apps/admin/src/ 2>/dev/null
    ```
    Expected: NO output. If any imports ARE found, STOP and add a `"description"` field to `packages/ui/package.json` instead of removing — then skip steps 2-4.

    Step 2 — Remove the dependency entry from apps/web/package.json FIRST (per Pitfall #5 — removing directory before dep entry causes npm install to fail):
    In `apps/web/package.json`, remove the line:
    ```
    "@broto/ui": "*"
    ```
    from the `"dependencies"` section.

    Step 3 — Update lockfile:
    ```
    npm install
    ```

    Step 4 — Remove the package directory:
    ```
    git rm -r packages/ui/
    ```

    Step 5 — Stage and commit:
    ```
    git add apps/web/package.json package-lock.json
    git commit -m "chore(hygiene): remove unused packages/ui package (HYGN-03)"
    ```
  </action>
  <verify>
    <automated>grep "@broto/ui" apps/web/package.json</automated>
    Expected: no output (dependency removed)
    <automated>ls packages/ui/ 2>/dev/null | wc -l</automated>
    Expected: 0 (directory gone)
  </verify>
  <acceptance_criteria>
    - `grep "@broto/ui" apps/web/package.json` returns empty (exit code 1)
    - `ls packages/ui/ 2>/dev/null` returns nothing (directory removed)
    - `git ls-files packages/ui/` returns empty (not tracked)
    - `npm install` exits 0 after the change (no missing workspace package error)
    - `grep -r "@broto/ui" apps/` returns empty (no imports anywhere — confirmed pre-removal)
  </acceptance_criteria>
  <done>packages/ui is removed from the monorepo. apps/web no longer lists it as a dependency. npm install succeeds.</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `git ls-files | grep -E "\.venv|__pycache__"` — must return empty
2. `ls 2.svg new_logo.svg new_logo_icon.svg 2>/dev/null` — must return nothing
3. `grep "@broto/ui" apps/web/package.json` — must return empty
4. `ls packages/ui 2>/dev/null` — must return nothing
5. `npm install` — must exit 0
</verification>

<success_criteria>
- Git index contains zero Python environment files
- All SVG assets used by apps are optimized (SVGO multipass applied)
- Root-level SVG duplicates deleted from repo
- packages/ui directory and its web package.json dependency entry both removed
- npm workspace resolves cleanly (no missing package errors)
</success_criteria>

<output>
After completion, create `.planning/phases/01-tooling-hygiene-security/01-01-SUMMARY.md` with:
- What was removed/optimized
- SVG size before/after (from ls -lh output)
- Confirmation that grep found no @broto/ui imports before removal
- Commit hashes for each of the 3 commits
</output>
