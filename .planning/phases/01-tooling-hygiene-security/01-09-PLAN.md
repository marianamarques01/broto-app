---
phase: 01-tooling-hygiene-security
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/lib/types/questions.ts (deletion)
  - apps/web/src/lib/types/questions.ts (deletion)
  - "apps/mobile/**/*.ts (import updates — 4 files)"
  - "apps/web/src/**/*.ts (import updates — 5 files)"
autonomous: true
requirements: [HYGN-04]

must_haves:
  truths:
    - "Build e navegação de tipos não dependem mais de wrappers lib/types/questions em nenhum app"
    - "Qualquer import de tipos de questões resolve diretamente para @broto/shared"
    - "Após as mudanças, não há regressão de compilação TypeScript nos apps afetados"
  artifacts:
    - path: "apps/mobile/lib/types/questions.ts"
      provides: "Wrapper removido do mobile"
      contains: "file does not exist"
    - path: "apps/web/src/lib/types/questions.ts"
      provides: "Wrapper removido do web"
      contains: "file does not exist"
  key_links:
    - from: "apps/mobile/**/*.{ts,tsx}"
      to: "@broto/shared"
      via: "imports atualizados para caminho direto"
      pattern: "from '@broto/shared'"
    - from: "apps/web/src/**/*.{ts,tsx}"
      to: "@broto/shared"
      via: "imports atualizados para caminho direto"
      pattern: "from '@broto/shared'"
---

<objective>
Remove os re-export files de questões e atualizar todos os callers para importar diretamente de `@broto/shared`.

Purpose: Os wrappers `lib/types/questions.ts` são ruído sem valor funcional e forçam import em duas etapas. Este plano remove dead code (per D-04) sem alterar comportamento de runtime.
Output: 2 arquivos wrapper removidos e imports atualizados nos consumidores mobile/web.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tooling-hygiene-security/1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover wrappers de questões e atualizar callers (D-04)</name>
  <files>
    apps/mobile/lib/types/questions.ts (deletion)
    apps/web/src/lib/types/questions.ts (deletion)
    "apps/mobile/**/*.ts (4 caller files — update imports)"
    "apps/web/src/**/*.ts (5 caller files — update imports)"
  </files>
  <read_first>
    - apps/mobile/lib/types/questions.ts (confirmar que é apenas re-export)
    - apps/web/src/lib/types/questions.ts (confirmar que é apenas re-export)
    - localizar todos os callers com `rg "lib/types/questions" apps/mobile apps/web/src`
  </read_first>
  <action>
    1) Confirmar que os dois arquivos contêm apenas re-export para `@broto/shared`.
    Se houver lógica adicional, interromper e reportar (não deletar).

    2) Atualizar todos os imports dos callers de:
    - `@/lib/types/questions`
    - `../lib/types/questions`
    para `@broto/shared`, preservando os símbolos importados.

    3) Deletar os dois wrappers:
    - `apps/mobile/lib/types/questions.ts`
    - `apps/web/src/lib/types/questions.ts`

    4) Rodar typecheck dos apps impactados:
    - `npx tsc --noEmit --project apps/web/tsconfig.json`
    - `npx tsc --noEmit --project apps/mobile/tsconfig.json`
  </action>
  <verify>
    <automated>ls apps/mobile/lib/types/questions.ts 2>/dev/null | wc -l</automated>
    <automated>ls apps/web/src/lib/types/questions.ts 2>/dev/null | wc -l</automated>
    <automated>rg "lib/types/questions" apps/mobile apps/web/src | wc -l</automated>
    <automated>npx tsc --noEmit --project apps/web/tsconfig.json</automated>
    <automated>npx tsc --noEmit --project apps/mobile/tsconfig.json</automated>
  </verify>
  <done>Wrappers removidos e todos os callers mobile/web importando tipos de questões diretamente de `@broto/shared`, sem regressão de typecheck.</done>
</task>

</tasks>

<verification>
1. `ls apps/mobile/lib/types/questions.ts 2>/dev/null | wc -l` retorna `0`.
2. `ls apps/web/src/lib/types/questions.ts 2>/dev/null | wc -l` retorna `0`.
3. `rg "lib/types/questions" apps/mobile apps/web/src` retorna vazio.
4. `npx tsc --noEmit --project apps/web/tsconfig.json` sai com código 0.
5. `npx tsc --noEmit --project apps/mobile/tsconfig.json` sai com código 0.
</verification>

<success_criteria>
- Não existem mais wrappers `lib/types/questions.ts` em mobile/web.
- Nenhum import restante usa `lib/types/questions`.
- Consumidores passam a importar de `@broto/shared` diretamente.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tooling-hygiene-security/01-09-SUMMARY.md` with:
- Lista de callers atualizados (mobile + web)
- Confirmação da remoção dos dois wrappers
- Resultado do typecheck de web e mobile
- Hash do commit
</output>
