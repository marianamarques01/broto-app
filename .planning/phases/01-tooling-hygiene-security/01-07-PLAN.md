---
phase: 01-tooling-hygiene-security
plan: 07
type: execute
wave: 4
depends_on: [01-04]
files_modified:
  - .git-blame-ignore-revs
autonomous: true
requirements: [TOOL-04]

must_haves:
  truths:
    - "Execuções de git blame nos arquivos reformulados deixam de apontar majoritariamente para o commit de formatação"
    - "O histórico diário fica legível porque o commit de style é ignorável via --ignore-revs-file"
  artifacts:
    - path: ".git-blame-ignore-revs"
      provides: "Formatting commit hash registration"
      contains: "40-character SHA from Plan 01-04"
  key_links:
    - from: ".git-blame-ignore-revs"
      to: "style: apply Prettier formatting across all apps (TOOL-02, TOOL-04)"
      via: "git rev-parse + file entry"
      pattern: "[0-9a-f]{40}"
---

<objective>
Register only the formatting commit hash in `.git-blame-ignore-revs`.

Purpose: This isolates blame metadata from formatting and rename work, keeping each change auditable and independently reversible.
Output: One commit with `.git-blame-ignore-revs` referencing the exact hash from plan `01-04`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tooling-hygiene-security/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar .git-blame-ignore-revs com o hash do commit de formatação (D-08)</name>
  <files>.git-blame-ignore-revs</files>
  <action>
    1) Obter o hash exato do commit do plano `01-04`:
    ```
    git log --grep="style: apply Prettier formatting across all apps" -n 1 --format=%H
    ```

    2) Criar/atualizar `.git-blame-ignore-revs` com comentários e o hash:
    ```
    # Formatting-only commits — ignore in blame
    <FORMAT_HASH>
    ```
    Substitua `<FORMAT_HASH>` pelo SHA obtido no passo anterior.

    3) Validar referência:
    ```
    git blame --ignore-revs-file .git-blame-ignore-revs package.json | head -5
    ```

    4) Commit isolado:
    ```
    git add .git-blame-ignore-revs
    git commit -m "chore(tooling): register format commit in .git-blame-ignore-revs (TOOL-04)"
    ```
  </action>
  <verify>
    <automated>cat .git-blame-ignore-revs | rg "^[0-9a-f]{40}$" | wc -l</automated>
  </verify>
  <done>`.git-blame-ignore-revs` existe e referencia o commit de formatação do plano `01-04`.</done>
</task>

</tasks>

<verification>
1. `cat .git-blame-ignore-revs | rg "^[0-9a-f]{40}$"` retorna 1 hash.
2. `git log --grep="register format commit in .git-blame-ignore-revs" -n 1 --oneline` retorna o commit do plano.
</verification>

<success_criteria>
- Blame-ignore fica separado do commit de formatação.
- O hash registrado corresponde ao commit gerado em `01-04`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tooling-hygiene-security/01-07-SUMMARY.md` with:
- SHA registrado
- comando de validação executado
- hash do commit deste plano
</output>
